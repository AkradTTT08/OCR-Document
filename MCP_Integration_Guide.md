# คู่มือการเชื่อมต่อ Spectra QA (MCP) เข้ากับ Claude Desktop

เอกสารฉบับนี้อธิบายวิธีการเชื่อมต่อ **Spectra QA Server** เข้ากับ **Claude Desktop** อย่างเป็นทางการ ผ่านมาตรฐาน Model Context Protocol (MCP) เพื่อให้ Claude สามารถดึงความสามารถของ Backend ของคุณไปใช้งานได้อัตโนมัติ

---

## 1. การตั้งค่าฝั่ง Claude Desktop (Configuration)

เพื่อให้ Claude Desktop รู้จักกับ MCP Server ของ Spectra QA คุณจำเป็นต้องเข้าไปแก้ไขไฟล์ตั้งค่า `claude_desktop_config.json`

### 📌 ตำแหน่งของไฟล์ตั้งค่า
- **Windows:** `%APPDATA%\Claude\claude_desktop_config.json` 
  *(สามารถเปิดผ่าน Run โดยกด `Win + R` แล้วพิมพ์ค่าดังกล่าว)*
- **Mac:** `~/Library/Application Support/Claude/claude_desktop_config.json`

### 📌 โค้ดที่ต้องนำไปใส่ (สำหรับเชื่อมต่อ 2 ระบบเข้าด้วยกัน)
ระบบที่สมบูรณ์จะต้องเชื่อมต่อ MCP ทั้ง 2 ตัวควบคู่กัน (ตัว RAG เดิม และตัว QA ใหม่) ให้คุณเพิ่ม/แก้ไขโครงสร้าง JSON ด้านล่างนี้:

```json
{
  "mcpServers": {
    "qa-agent-server": {
      "command": "docker",
      "args": ["exec", "-i", "qa_agent_mcp", "python", "agent_mcp_server.py"]
    },
    "spectra-qa-mcp": {
      "command": "python",
      "args": ["d:/OCR/backend/mcp_server.py"]
    }
  }
}
```
*(**หมายเหตุ:** `qa-agent-server` คือตัว Database AIAgentQA ที่รันใน Docker ส่วน `spectra-qa-mcp` คือระบบประเมิน Exit Criteria และ OCR หากคุณมีวิธีรันต่างออกไป สามารถปรับแก้ command/args ได้ตามต้องการ)*

เมื่อบันทึกไฟล์เสร็จเรียบร้อย **ให้ปิดโปรแกรม Claude Desktop อย่างสมบูรณ์แล้วเปิดขึ้นมาใหม่** (Restart Claude Desktop)

---

## 2. เครื่องมือ (Tools) ที่มีให้ใช้งานผ่าน MCP

เมื่อเปิด Claude Desktop ขึ้นมาใหม่ สัญลักษณ์ "ค้อน" (Tools) จะปรากฏขึ้น ซึ่งบ่งบอกว่า Claude เชื่อมต่อกับ Backend สำเร็จแล้ว โดยจะมีเครื่องมือให้ใช้งานร่วมกันจากทั้ง 2 ฝั่ง (เปรียบเสมือนแขนซ้ายและแขนขวา) ดังนี้:

### 🗄️ แขนซ้าย: Database AIAgentQA (ตัว RAG ดั้งเดิม)
เครื่องมือสำหรับการสืบค้นข้อมูลองค์กร ฐานข้อมูลโปรเจกต์ และดึง Context มาให้ AI อ่านก่อนร่างเอกสาร

- **`list_projects` / `list_project_documents`:** ใช้ดูรายชื่อโปรเจกต์ทั้งหมดที่กำลัง Active และดูรายการเอกสารที่เกี่ยวข้อง เพื่อสืบค้นข้อมูลตั้งต้น
- **`list_skills` / `get_skill`:** ดึงคู่มือหรือคำสั่ง (Prompt/Markdown Instructions) สำหรับ AI Skill เฉพาะทาง (เช่น กฎการเขียน Requirement หรือ Manual)
- **`search_project_documents`:** ทำ Vector Search (RAG) ค้นหาข้อมูลเชิงลึก สถิติ หรือเนื้อหาที่เคยเขียนไว้ในโปรเจกต์มาใช้อ้างอิง (Golden Data)
- **`get_document_for_review` / `save_qa_evaluation`:** ดึงเนื้อหาเอกสารฉบับเต็มมาอ่าน และบันทึกประวัติการประเมินลง Database

### ⚙️ แขนขวา: Spectra QA (ตัวประเมินและแจ้งเตือน)
เครื่องมือสำหรับจัดการไฟล์, ตรวจสอบความถูกต้อง, และการสื่อสาร (Action Tools)

### 🔍 1. `ocr_document` (ดึงข้อมูลจากเอกสารรูปภาพ/PDF)
- **หน้าที่:** ใช้ดึง Text (Markdown) ออกมาจากไฟล์ PDF หรือรูปภาพ ไม่ว่าจะเป็นไฟล์บนเครื่อง (Local) หรือผ่าน URL (โดยมีการรองรับระบบ SSRF Protection)
- **เมื่อไหร่ที่ Claude จะใช้:** เมื่อคุณส่งรูปหรือลิงก์ PDF ให้ Claude และสั่งให้ "อ่านเอกสารนี้ให้หน่อย"

### 🛡️ 2. `evaluate_spectra_qa` (ระบบ AI Agent QA Loop)
- **หน้าที่:** นำเนื้อหาเอกสาร (Draft) ที่ Claude แต่งเสร็จ ส่งเข้าไปประเมินเทียบกับ Exit Criteria Checklist ของโปรเจกต์คุณ
- **เมื่อไหร่ที่ Claude จะใช้:** เมื่อ Claude ร่างเอกสารเสร็จ และต้องการตรวจสอบความถูกต้องว่าผ่านเกณฑ์ของ Spectra QA หรือไม่ หากตกเกณฑ์ ระบบจะบอกจุดผิดพลาด เพื่อให้ Claude นำไปแก้ไขเองอัตโนมัติ (รองรับระบบ Circuit Breaker ป้องกันการติดลูป)

### ✉️ 3. `send_email_report` (ระบบส่งอีเมลอัตโนมัติ)
- **หน้าที่:** สั่งให้ระบบ Backend ยิงอีเมลพร้อมแนบผลลัพธ์/รายงานไปยังอีเมลปลายทาง
- **เมื่อไหร่ที่ Claude จะใช้:** เมื่อเอกสารผ่านเกณฑ์ Exit Criteria แล้ว (APPROVED) Claude จะเรียกเครื่องมือนี้เพื่อทำการส่งอีเมลแจ้งเตือนคุณแบบจบกระบวนการ

---

## 3. วิธีการสั่งงานบน Claude (System Prompt & Usage)

หลังจากเชื่อมต่อเสร็จแล้ว สิ่งสำคัญที่จะทำให้ระบบทำงานเป็น "Fully Autonomous" คือการเขียน Prompt ให้ Claude เข้าใจกระบวนการ (Workflow) คุณสามารถใช้ข้อความด้านล่างนี้ เป็น Prompt สั่งงานหรือตั้งเป็น System Prompt (Project Instructions) ได้เลย:

### 🎯 ตัวอย่างคำสั่ง (Prompt) ที่ให้ Claude ทำงานแบบครบวงจร

> **Role:**
> คุณคือ AI ผู้ช่วยอัจฉริยะ (Spectra QA Agent) ที่มีความสามารถในการอ่านเอกสาร เขียน/แก้ไขเอกสาร และตรวจสอบเกณฑ์คุณภาพ
>
> **Workflow (บังคับให้ทำตามลำดับอย่างเคร่งครัด):**
> 1. หากผู้ใช้แนบลิงก์ไฟล์หรือที่อยู่ไฟล์ ให้คุณใช้ Tool `ocr_document` เพื่อดึงข้อมูล Text ออกมาก่อน
> 2. สรุป/ร่างเอกสารตามคำสั่งของผู้ใช้ (เช่น ร่างเป็นประเภท 'Requirement' หรือ 'Manual')
> 3. เมื่อร่างเอกสารเสร็จแล้ว ห้ามเพิ่งส่งให้ผู้ใช้ดู ให้คุณเรียกใช้ Tool `evaluate_spectra_qa` โดยส่งเนื้อหาทั้งหมด (document_content) เข้าไปตรวจสอบ
> 4. หากผลการประเมินแจ้งว่า 'REJECTED' ให้คุณอ่าน `failed_criteria` ที่ระบบคืนมา แล้วทำการแก้ไขเอกสารให้ถูกต้อง จากนั้นส่งไปประเมินด้วย `evaluate_spectra_qa` อีกครั้ง (วนลูปจนกว่าจะผ่าน หรือจนกว่าระบบจะแจ้งเตือน Circuit Breaker Hit)
> 5. เมื่อได้รับผลการประเมินเป็น 'PASS' ให้คุณใช้ Tool `send_email_report` เพื่อส่งเนื้อหาที่สมบูรณ์และสรุปผลการผ่านเกณฑ์ไปที่อีเมลที่ผู้ใช้กำหนด
> 6. เมื่อเสร็จสิ้นกระบวนการทั้งหมด ค่อยพิมพ์แจ้งผู้ใช้ในช่องแชท พร้อมสรุปผล และพิมพ์เนื้อหาเอกสารฉบับไฟนอลออกมา

---

## 💡 สรุปขั้นตอนการทำงาน (The Execution Flow)

หากคุณใช้ Prompt ด้านบน เมื่อคุณทัก Claude ว่า **"ช่วยร่าง Requirement สำหรับระบบ Login ให้หน่อย และส่งเมลไปที่ ceo@test.com"** สิ่งที่จะเกิดขึ้นคือ:

1. **[Drafting]** Claude แอบร่าง Requirement ให้คุณเงียบๆ
2. **[QA Loop]** Claude ส่งที่ร่างไปหา Spectra QA ผ่าน MCP ระบบตีกลับมาว่า *"ไม่ผ่านเกณฑ์ข้อ 2 (ข้อมูลสถิติไม่ครบ)"* Claude ก็จะแอบแก้ข้อมูลแล้วส่งไปตรวจใหม่
3. **[Approved]** เมื่อ Spectra QA ตอบกลับว่าสถานะคือ `PASS` (ผ่านเกณฑ์)
4. **[Dispatch]** Claude สั่งให้ยิงอีเมลผลลัพธ์ไปที่ ceo@test.com
5. **[Finished]** Claude ตอบกลับคุณหน้าจอแชทว่า *"เรียบร้อยครับ เอกสารผ่านการประเมินและผมส่งอีเมลให้แล้ว นี่คือตัวเอกสารฉบับไฟนอล..."*

---

## <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/c/c5/Claude_AI_logo.svg/1024px-Claude_AI_logo.svg.png" width="30" style="border-radius: 5px; vertical-align: middle;"> Tutorial: วิธีการใช้งานสำหรับ User ทั่วไป

### ขั้นตอนที่ 1: ตรวจสอบการเชื่อมต่อ (ค้อนคู่วิเศษ)
เมื่อคุณเปิดโปรแกรม **Claude Desktop** สังเกตที่มุมล่างขวาของช่องพิมพ์ข้อความ (Chat box) จะต้องมีสัญลักษณ์ **"รูปค้อน" (Tools)** ปรากฏอยู่ หากคลิกดูจะพบเครื่องมือทั้งหมดที่เราเชื่อมต่อไว้ (เช่น `list_projects`, `evaluate_spectra_qa`)

### ขั้นตอนที่ 2: เริ่มต้นออกคำสั่ง (Prompting)
คุณไม่จำเป็นต้องกดใช้ Tool เอง! เพียงแค่พิมพ์สั่งงานด้วยภาษามนุษย์ปกติ แล้วบอกเงื่อนไขให้ครบถ้วน เช่น:
> *"ช่วยร่างเอกสารแบบ Requirement สำหรับโปรเจกต์ 'ระบบ HR' ให้หน่อย โดยอ้างอิงข้อมูลจากโปรเจกต์ที่มีอยู่ในระบบ และเมื่อร่างเสร็จแล้ว ให้ตรวจ Exit Criteria ให้ผ่านทั้งหมด แล้วส่งอีเมลไปที่ admin@company.com"*

### ขั้นตอนที่ 3: ปล่อยให้ AI ทำงาน (Autonomous Mode)
หลังจากส่งคำสั่ง คุณจะเห็น Claude **ขึ้นไอคอนหมุนๆ ว่ากำลังใช้งาน Tool** (เช่น กำลัง `search_project_documents` หรือ `evaluate_spectra_qa`) โดยที่คุณไม่ต้องทำอะไรเลย:
- หากมีข้อมูลผิดพลาด Claude จะ **"อ่าน Error"** จากระบบ และแก้ไขเอกสารตัวเองแบบอัตโนมัติ
- คุณสามารถนั่งรอจนกว่าข้อความสุดท้ายของ Claude จะเด้งขึ้นมาบอกว่า *"ส่งอีเมลสำเร็จแล้ว!"*

**ทั้งหมดนี้เกิดขึ้นด้วยการพิมพ์คำสั่งเพียงครั้งเดียวของคุณ!**
